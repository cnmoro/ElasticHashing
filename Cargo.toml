[package]
name = "elastic_hash"
version = "1.0.0"
edition = "2021"

[lib]
name = "elastic_hash"
crate-type = ["cdylib"]

[dependencies]
# Updated to 0.23 to support Python 3.13
pyo3 = { version = "0.23", features = ["extension-module"] }
rand = "0.8"
ahash = "0.8"